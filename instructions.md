start bot
check if there is signal when triggered
after determining that there is signal, check if there is an existing session
if there is an existing session, fetch data for the session and determine which candle is being traded
fecth the previously completed candle for the symbol in that session
if the first candle has not been traded in that sesision, calculate the amount that 
will be needed to trade this first candle.
the system must store candle being traded for session. 
